# MEMORY.md

Root project memory for future agents. Keep this file compact and move detailed area notes into
scoped `MEMORY.md` files.

## Project Goal

Create a local-first cohort variant browser for WGS and WES data. The system should ingest VCF/GVCF
files, store genotypes in Hail VDS, compute database-level allele frequencies, and expose variant
and gene-level frequency queries through a patched gnomAD browser.

## Stable Decisions

- VDS is the durable genotype store.
- Annotated MatrixTable is the downstream annotation and export layer.
- Cohort AF comes from Hail `variant_qc()` and should expose `ac_total`, `an_total`, `af_total`,
  and `hom_count`.
- Current local/demo browser serving uses Elasticsearch index `cohort_variants`; production should
  split internal and public indexes.
- The tracked source for browser changes is `browser/`; `gnomad-browser/` is generated by
  `setup.sh`.
- Production security model has two database tiers: an internal patient-linked database on an
  internal analysis VM, and a separate public/sanitized variant database on a different browser VM.
- The public database is populated only from an explicit allowlisted export and must not contain
  raw GVCFs, VDS, MatrixTables, patient IDs, `sample_id`, per-sample genotypes, clinical metadata,
  dates, HPO assignments, report flags, run IDs, care sites, or internal paths.
- Current reference assumptions from project docs: GRCh37/hg19, VEP 108, gnomAD v2.1.1 exomes.
- Annotation sources decided: VEP (core) + dbNSFP v5.3.1 for functional predictors including
  CADD + ClinVar (local VCF join) + gnomAD v2.1.1 exomes (local HT join).
- Internal browser deployment: internal network only, HTTPS, with authentication (oauth2-proxy or
  basic auth). Public browser deployment must use only the sanitized public index.

## Input References

User-provided planning files:

- `/Users/markus/Downloads/test_samples_data.csv`
- `/Users/markus/Downloads/test_panels_data.csv`
- `/Users/markus/Downloads/andmebaasi_struktuur_27032026.pdf`
- `/Users/markus/Downloads/clingen_browser_overview (1).pdf`

Current test sample CSV fields (updated schema — CSV files not yet updated):

- `sample_id`
- `chromosomal_sex` (was `sex` — inferred from genotype data; XX, XY, ambiguous)
- `sex_assigned` (was `sex` — from clinical metadata; Male, Female, Other, Unknown)
- `date_of_birth` (was `age` — ISO date string YYYY-MM-DD)
- `date_seq`
- `run_id`
- `care_site`
- `health_status`
- `material` (new — blood, tissue, saliva, buccal swab, etc.)

Current test panel CSV fields:

- `sample_id`
- `panel`

## Metadata Rules

- `sample_id` is the join key between genomic data, sample metadata, and panels.
- Panels are represented as one row per sample and panel. Multiple rows for one sample are valid.
- Preserve both social self-defined sex and chromosomal sex as separate concepts.
- Validate panels-only sample IDs and likely sample ID typos before merging metadata.
- Normalize date fields before loading them into Hail or Elasticsearch.

## Scoped Memory

- `docs/pipeline/MEMORY.md`: Hail/VDS/MatrixTable, metadata merge, AF, fixture rules.
- `browser/MEMORY.md`: Elasticsearch schema, GraphQL routing, browser dataset assumptions.
- `memory/project_pipeline_status.md`: dated server pipeline status.
- `memory/project_server_infrastructure.md`: dated server paths and Podman/Docker notes.
- `memory/project_packaging_decision.md`: dated packaging decision.

## Architecture Docs

- `docs/ARCHITECTURE.md`: full data model, annotation strategy, deployment and security spec.
- `docs/VM_STRUCTURE.md`: canonical VM directory layout and migration steps.
- `docs/BROWSER_USE_CASES.md`: six target browser query scenarios with ES queries and gap analysis.
- `annotation_sources.md`: per-field source reference with paths and integration details.

## Open Questions

- Exact GE server export/API contract, refresh cadence, and authoritative metadata columns.
- HPO filter UI exposure: first local demo or later phase.
- dbNSFP integration method: VEP plugin (single pass) vs. standalone Hail Table join.
- Exact dbNSFP field allowlist for MatrixTable, internal Elasticsearch, public Elasticsearch, and UI.
- Auth method for browser: oauth2-proxy vs. HTTP basic auth vs. VPN-only.
- Exact public export field allowlist, Elasticsearch index name, one-way transfer method, and rare
  variant privacy thresholds.
- Whether `chromosomal_sex` inference runs in `annotate_cohort.py` or as a separate pre-processing step.
- Gene-level frequency aggregation shape for browser queries.
- Whether tiny fixture VCF/GVCF files may be committed or must remain local and ignored.
